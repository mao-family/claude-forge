# Local secrets (machine-specific)
settings.local.json

# OS files
.DS_Store
*.tmp

# Workspace repos (cloned separately)
workspace/repos/*
!workspace/repos/.gitkeep

# Git worktrees
.worktrees/

# Claude Code project-level config (if created)
.claude/
